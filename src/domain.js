/**
 * Shared vocabulary of the landing, aligned with the demo-requests API contract.
 */
export const PROFILES = Object.freeze(['admin', 'guest']);
export const ACCOMMODATION_TYPES = Object.freeze(['boutique', 'alternative', 'chain']);
export const ROOMS_RANGES = Object.freeze(['1-10', '11-30', '31-60', '60+']);
export const REFERRAL_SOURCES = Object.freeze(['search', 'social', 'referral', 'event', 'other']);
